import 'package:flutter/material.dart';

class BalanceUpdateScreen extends StatelessWidget {
  const BalanceUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Balance Update')),
      body: const Center(child: Text('Balance Update Screen')),
    );
  }
}
